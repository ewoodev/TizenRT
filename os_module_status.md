# `os/` Module Status

| Module | Scope in This Worktree | Function Docs | Header Comments | `AGENTS.md` | Commit | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `os/logm` | Full public slice from `tinyara/logm.h` | `done` | `comment_fixed` | `done` | `efbf42eea` | Small self-contained module, good first commit target. |
| `os/compression` | Full public slice from `tinyara/compression.h` and `tinyara/binfmt/compression/compress_read.h` except `compress_register` | `done` | `comment_fixed` | `done` | `051ed6a82` | Includes compressed-binary helpers. |
| `os/drivers/compression` | `compress_register` and driver summary | `done` | `comment_fixed` | `done` | `13f407019` | Depends on `/dev/compress` ioctl behavior. |
| `os/crypto` | Full public slice is done: `getrandom`, the random-pool helper family, all six public BLAKE2s entry points, and the module guide | `done` | `comment_fixed` | `done` | `c09651bab` | `up_rngaddint()`, `up_rngaddentropy()`, `up_rngreseed()`, and `up_randompool_initialize()` were completed in `44d5a526a`; `blake2s()`, `blake2s_init()`, `blake2s_update()`, and `blake2s_final()` were completed in `dc8ea6326`; `blake2s_init_key()` and `blake2s_init_param()` were completed in `c09651bab`, which also fixes delegated failure propagation in `blake2s_init_key()`. |
| `os/arch` | `boardctl` plus the module guide are done; board-private implementation tracing remains excluded | `done` | `comment_fixed` | `done` | `eb633f7ab` | `os/arch/AGENTS.md` now frames this folder as the common `boardctl()` dispatch layer and records that `BOARDIOC_INIT` currently ignores the public `arg` value. |
| `os/kernel/errno` | Full public slice from `errno.h` function declarations | `done` | `comment_fixed` | `done` | `f0668d187` | Small module and next full-closure target. |
| `os/kernel/semaphore` | Folder-owned public semaphore APIs are done: destroy/wait/trywait/timedwait/timeout/post/tickwait/reset/setprotocol/register/unregister plus the module guide | `done` | `comment_fixed` | `done` | `f2ea740ef` | `sem_init()`, `sem_getvalue()`, and `sem_getprotocol()` remain outside this folder in `lib/libc/semaphore`. Named semaphore APIs stay pending in `os/fs/semaphore`. |
| `os/fs/semaphore` | Named semaphore APIs `sem_open()`, `sem_close()`, and `sem_unlink()` plus the module guide are done | `done` | `comment_fixed` | `done` | `64d9f4453` | Named semaphores are VFS/inode-backed. The current implementation ignores `mode`, defers destruction until unlink plus final close, and uses the fixed `MAX_SEMPATH` buffer for name resolution. |
| `os/mm/mm_gran` | Full folder-owned allocator slice is done: `gran_initialize()`, `gran_release()`, `gran_reserve()`, `gran_alloc()`, `gran_free()`, `mm_pginitialize()`, `mm_pgreserve()`, `mm_pgalloc()`, `mm_pgfree()`, and the module guide | `done` | `comment_fixed` | `done` | `ce80ebd49` | The gran tranche corrected `CONFIG_GRAN_SINGLE` public comment mismatches and fixed two allocator bugs in `gran_alloc()`: an out-of-bounds read of the next GAT word on the last bitmap block and an uninitialized failure-log counter. The page-wrapper tranche aligned `tinyara/pgalloc.h` with the real gran-based wrapper semantics, fixed the non-`CONFIG_GRAN_SINGLE` assertion typo in `mm_pginitialize()`, and corrected `mm_pgalloc()` so it requests `npages << MM_PGSHIFT` bytes instead of always allocating one page. |
| `os/fs/aio` | Folder-owned public AIO APIs `aio_cancel()`, `aio_fsync()`, `aio_read()`, and `aio_write()` plus the module guide are done | `done` | `comment_fixed` | `done` | `a3bc9001f` | The tranche corrected `aio.h` to describe the LPWORK-based owner implementation, documented the caller-owned aiocb/buffer contract, and fixed queue-failure cleanup plus worker-side use-after-free of the cached file pointer. |
| `os/fs/mqueue` | Folder-owned public named-queue lifecycle APIs `mq_open()`, `mq_close()`, `mq_close_group()`, and `mq_unlink()` plus the module guide are done | `done` | `comment_fixed` | `done` | `df0ad4e98` | The tranche aligned `mqueue.h` and `tinyara/mqueue.h` comments with the inode-backed lifetime model, added runtime `EBADF` handling for invalid close paths, rejected `NULL` and overlong queue names in `mq_open()`/`mq_unlink()`, and now propagates unexpected `inode_remove()` failures from `mq_unlink()`. |
| `os/kernel/mqueue` | Full kernel-owned message-queue slice is done: send/receive/timed send/timed receive, notify, setattr/getattr, public helpers, and the module guide | `done` | `comment_fixed` | `done` | `d9bc5b469` | Helper APIs were completed in `7d7b0712d` with the real queue-object/descriptor lifetime split, default queue-limit handling, and no-`errno` NULL failure contracts. The module guide closed the folder in `d9bc5b469`. |
| `os/kernel/signal` | Control APIs, the `tinyara/signal.h` helper pair, send APIs, and wait APIs are done; the module guide remains pending | `done` | `comment_fixed` | `pending_dependency` | `de42b05ce` | The control tranche aligned `signal.h` comments with the task-local implementation, fixed the `CONFIG_SIGKILL_HANDLER` query path in `sigaction()`, and corrected `sigprocmask()` so invalid `how` now sets `EINVAL` and does not dispatch pending signals on that failure path. The helper tranche documented the target-task helper contracts, fixed the `ENOMEM` scheduler-lock leak in `sig_sethandler()`, and corrected repeated `sig_sethandler()` calls so they replace the existing entry instead of leaving the older handler active. The send tranche documented `kill()` and `sigqueue()`, fixed `kill(signo == 0)` so it now performs existence checking without dispatch, and rejected `signo == 0` in `sigqueue()`. The wait tranche documented `sigsuspend()`, `sigwaitinfo()`, and `sigtimedwait()`, fixed `sigsuspend()` so already pending unmasked signals are dispatched instead of dropped and now return `EINTR`, and made `sigtimedwait()` reject invalid `tv_nsec` values and report waitdog-allocation failure as `ENOMEM`. Next up is the module guide. |
| `os/fs/dirent` | Full os-backed public directory stream slice: `opendir`, `closedir`, `readdir`, `rewinddir`, `seekdir` | `done` | `comment_fixed` | `done` | `7e70b1cc5` | `readdir_r()` and `telldir()` stay pending because their implementations live in `lib/libc/dirent`. |
| `os/fs/vfs` | `ioctl`, core FD I/O, descriptor-control, pathname/metadata, filesystem-capacity, readiness APIs, poll helpers, and the module guide are done | `done` | `comment_fixed` | `done` | `9b584411d` | Core FD I/O was committed in `5bb2406f0`; descriptor-control was closed in `11fea1063`; pathname/metadata was committed in `b198ffe76` and refined in `32f36f092`; filesystem-capacity was committed in `d2ee2864f` and refined in `deb662806`; readiness APIs were committed in `ea61d21eb` and `e618cb415`; poll-helper APIs were committed in `1b59dad7e` and refined in `0fe90f6f7`. The module guide was committed in `9b584411d`. |
| `os/net` | Bridge/control APIs, helper/init APIs, and the module guide are done; `netdev_foreach()` is now tracked as an owner-missing declaration under `os/`, and broader public net APIs remain pending later | `in_progress` | `in_progress` | `done` | `88459c6bc` | `net_poll()` was committed in `409b6e1d8` and refined in `03b117d2f`. `net_close()`, `net_dupsd()`, `net_dupsd2()`, and `net_clone()` were committed in `152ddc4ba`. `net_vfcntl()` was committed in `37dfc15c6`. `net_ioctl()` was committed in `4f85c85f0`. Helper/init APIs were completed in `88459c6bc` with the current lwIP `checksd` stub and socket-list semaphore lifecycle documented. The module guide was committed in `3bb35ed32`. The `netdev_foreach()` declaration remains unresolved because no matching `os/` owner implementation was found in the current worktree. |
| `os/kernel/clock` | Full os-backed public clock slice: `gettimeofday`, `clock_settime`, `clock_gettime`, `clock_getres`, `clock` | `done` | `comment_fixed` | `done` | `3331008ab` | Latest refinement aligned the remaining internal clock helper comments; `time()` remains out of scope because it is implemented in `lib/libc/time`. |
| `os/kernel/timer` | Full public slice from `time.h` timer declarations | `done` | `comment_fixed` | `done` | `2b4dcebd2` | Documented and refined the enabled path gated by `CONFIG_DISABLE_POSIX_TIMERS`, including source/header comment alignment. |
| `os/board` | Summary-only track for the public `board_*` family; summary guide is complete and implementation tracing remains excluded | `board_summary` | `comment_fixed` | `done` | `5d777624c` | `os/board/AGENTS.md` closes the summary track and records that stale boardctl-dispatch comments in `tinyara/board.h` were narrowed to match the common dispatcher. |
